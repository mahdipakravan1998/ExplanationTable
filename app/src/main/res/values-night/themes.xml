<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ExplanationTable" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorOnPrimary">@color/colorPrimaryText</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorHighlight</item>
        <item name="colorOnSecondary">@color/colorSecondaryText</item>

        <!-- Error colors -->
        <item name="colorError">@color/colorErrorWarning</item>
        <item name="colorOnError">@color/colorPrimaryText</item>

        <!-- Background color -->
        <item name="android:colorBackground">@color/colorBackgroundOverlay</item>

        <item name="colorSuccess">@color/colorSuccess</item>
    </style>
</resources>
